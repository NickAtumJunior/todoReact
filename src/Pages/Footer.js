import React from 'react';
import Button from '@mui/material/Button';
function Footer() {
    return (
        <div>
            <center><Button variant="contained">Clear</Button> <Button variant="contained">Add</Button></center>  
        </div>
    );
}

export default Footer;